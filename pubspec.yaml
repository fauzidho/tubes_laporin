name: project_tubes_apb
description: "LaporIn - Integrated Campus Facility Reporting System"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  google_fonts: ^6.2.1
  camera: ^0.11.0+2
  image_picker: ^1.1.2
  intl: ^0.19.0
  uuid: ^4.5.1
  path_provider: ^2.1.4
  path: ^1.9.0
  firebase_core: ^4.6.0
  firebase_auth: ^6.3.0
  cloud_firestore: ^6.2.0
  firebase_storage: ^13.2.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
